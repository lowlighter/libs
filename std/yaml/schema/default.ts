import { DEFAULT_SCHEMA as _variable_DEFAULT_SCHEMA } from "jsr:@std/yaml@0.224.3/schema/default"
/**
 * Default YAML schema. It is not described in the YAML specification.
 */
const DEFAULT_SCHEMA = _variable_DEFAULT_SCHEMA
export { DEFAULT_SCHEMA }

import { def as _variable_def } from "jsr:@std/yaml@0.224.3/schema/default"
/**
 * Default YAML schema. It is not described in the YAML specification.
 *
 * @deprecated This will be removed in 1.0.0. Use {@link DEFAULT_SCHEMA} instead.
 */
const def = _variable_def
export { def }
