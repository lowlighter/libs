// Imports
import octicons from "@primer/octicons"

/** Options for rendering an SVG icon. */
export type SVGOptions = {
  /** CSS class names to apply to the SVG element */
  class?: string
  /** Accessibility aria-label to apply to the SVG element */
  ariaLabel?: string
  /** Width of the SVG element */
  width?: number
  /** Height of the SVG element */
  height?: number
}

/** {@link https://primer.style/octicons/icon/accessibility-16 | accessibility icon}. */
export function accessibility(options?: SVGOptions): string {
  return octicons["accessibility"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/accessibility-inset-16 | accessibility-inset icon}. */
export function accessibilityInset(options?: SVGOptions): string {
  return octicons["accessibility-inset"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/agent-16 | agent icon}. */
export function agent(options?: SVGOptions): string {
  return octicons["agent"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/ai-model-16 | ai-model icon}. */
export function aiModel(options?: SVGOptions): string {
  return octicons["ai-model"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/alert-16 | alert icon}. */
export function alert(options?: SVGOptions): string {
  return octicons["alert"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/alert-fill-16 | alert-fill icon}. */
export function alertFill(options?: SVGOptions): string {
  return octicons["alert-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/apps-16 | apps icon}. */
export function apps(options?: SVGOptions): string {
  return octicons["apps"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/archive-16 | archive icon}. */
export function archive(options?: SVGOptions): string {
  return octicons["archive"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-both-16 | arrow-both icon}. */
export function arrowBoth(options?: SVGOptions): string {
  return octicons["arrow-both"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-down-16 | arrow-down icon}. */
export function arrowDown(options?: SVGOptions): string {
  return octicons["arrow-down"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-down-left-16 | arrow-down-left icon}. */
export function arrowDownLeft(options?: SVGOptions): string {
  return octicons["arrow-down-left"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-down-right-16 | arrow-down-right icon}. */
export function arrowDownRight(options?: SVGOptions): string {
  return octicons["arrow-down-right"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-left-16 | arrow-left icon}. */
export function arrowLeft(options?: SVGOptions): string {
  return octicons["arrow-left"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-right-16 | arrow-right icon}. */
export function arrowRight(options?: SVGOptions): string {
  return octicons["arrow-right"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-switch-16 | arrow-switch icon}. */
export function arrowSwitch(options?: SVGOptions): string {
  return octicons["arrow-switch"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-up-16 | arrow-up icon}. */
export function arrowUp(options?: SVGOptions): string {
  return octicons["arrow-up"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-up-left-16 | arrow-up-left icon}. */
export function arrowUpLeft(options?: SVGOptions): string {
  return octicons["arrow-up-left"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/arrow-up-right-16 | arrow-up-right icon}. */
export function arrowUpRight(options?: SVGOptions): string {
  return octicons["arrow-up-right"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/beaker-16 | beaker icon}. */
export function beaker(options?: SVGOptions): string {
  return octicons["beaker"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bell-16 | bell icon}. */
export function bell(options?: SVGOptions): string {
  return octicons["bell"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bell-fill-16 | bell-fill icon}. */
export function bellFill(options?: SVGOptions): string {
  return octicons["bell-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bell-slash-16 | bell-slash icon}. */
export function bellSlash(options?: SVGOptions): string {
  return octicons["bell-slash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/blocked-16 | blocked icon}. */
export function blocked(options?: SVGOptions): string {
  return octicons["blocked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bold-16 | bold icon}. */
export function bold(options?: SVGOptions): string {
  return octicons["bold"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/book-16 | book icon}. */
export function book(options?: SVGOptions): string {
  return octicons["book"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/book-locked-16 | book-locked icon}. */
export function bookLocked(options?: SVGOptions): string {
  return octicons["book-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bookmark-16 | bookmark icon}. */
export function bookmark(options?: SVGOptions): string {
  return octicons["bookmark"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bookmark-fill-16 | bookmark-fill icon}. */
export function bookmarkFill(options?: SVGOptions): string {
  return octicons["bookmark-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bookmark-filled-16 | bookmark-filled icon}. */
export function bookmarkFilled(options?: SVGOptions): string {
  return octicons["bookmark-filled"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bookmark-slash-16 | bookmark-slash icon}. */
export function bookmarkSlash(options?: SVGOptions): string {
  return octicons["bookmark-slash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bookmark-slash-fill-16 | bookmark-slash-fill icon}. */
export function bookmarkSlashFill(options?: SVGOptions): string {
  return octicons["bookmark-slash-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/boolean-off-16 | boolean-off icon}. */
export function booleanOff(options?: SVGOptions): string {
  return octicons["boolean-off"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/boolean-on-16 | boolean-on icon}. */
export function booleanOn(options?: SVGOptions): string {
  return octicons["boolean-on"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/briefcase-16 | briefcase icon}. */
export function briefcase(options?: SVGOptions): string {
  return octicons["briefcase"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/broadcast-16 | broadcast icon}. */
export function broadcast(options?: SVGOptions): string {
  return octicons["broadcast"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/browser-16 | browser icon}. */
export function browser(options?: SVGOptions): string {
  return octicons["browser"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/bug-16 | bug icon}. */
export function bug(options?: SVGOptions): string {
  return octicons["bug"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/cache-16 | cache icon}. */
export function cache(options?: SVGOptions): string {
  return octicons["cache"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/calendar-16 | calendar icon}. */
export function calendar(options?: SVGOptions): string {
  return octicons["calendar"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/check-16 | check icon}. */
export function check(options?: SVGOptions): string {
  return octicons["check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/check-circle-16 | check-circle icon}. */
export function checkCircle(options?: SVGOptions): string {
  return octicons["check-circle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/check-circle-fill-16 | check-circle-fill icon}. */
export function checkCircleFill(options?: SVGOptions): string {
  return octicons["check-circle-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/checkbox-16 | checkbox icon}. */
export function checkbox(options?: SVGOptions): string {
  return octicons["checkbox"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/checkbox-fill-16 | checkbox-fill icon}. */
export function checkboxFill(options?: SVGOptions): string {
  return octicons["checkbox-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/checklist-16 | checklist icon}. */
export function checklist(options?: SVGOptions): string {
  return octicons["checklist"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/chevron-down-16 | chevron-down icon}. */
export function chevronDown(options?: SVGOptions): string {
  return octicons["chevron-down"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/chevron-left-16 | chevron-left icon}. */
export function chevronLeft(options?: SVGOptions): string {
  return octicons["chevron-left"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/chevron-right-16 | chevron-right icon}. */
export function chevronRight(options?: SVGOptions): string {
  return octicons["chevron-right"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/chevron-up-16 | chevron-up icon}. */
export function chevronUp(options?: SVGOptions): string {
  return octicons["chevron-up"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/circle-16 | circle icon}. */
export function circle(options?: SVGOptions): string {
  return octicons["circle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/circle-slash-16 | circle-slash icon}. */
export function circleSlash(options?: SVGOptions): string {
  return octicons["circle-slash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/clock-16 | clock icon}. */
export function clock(options?: SVGOptions): string {
  return octicons["clock"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/clock-fill-16 | clock-fill icon}. */
export function clockFill(options?: SVGOptions): string {
  return octicons["clock-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/cloud-16 | cloud icon}. */
export function cloud(options?: SVGOptions): string {
  return octicons["cloud"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/cloud-offline-16 | cloud-offline icon}. */
export function cloudOffline(options?: SVGOptions): string {
  return octicons["cloud-offline"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/code-16 | code icon}. */
export function code(options?: SVGOptions): string {
  return octicons["code"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/code-of-conduct-16 | code-of-conduct icon}. */
export function codeOfConduct(options?: SVGOptions): string {
  return octicons["code-of-conduct"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/code-review-16 | code-review icon}. */
export function codeReview(options?: SVGOptions): string {
  return octicons["code-review"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/code-square-16 | code-square icon}. */
export function codeSquare(options?: SVGOptions): string {
  return octicons["code-square"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/codescan-16 | codescan icon}. */
export function codescan(options?: SVGOptions): string {
  return octicons["codescan"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/codescan-checkmark-16 | codescan-checkmark icon}. */
export function codescanCheckmark(options?: SVGOptions): string {
  return octicons["codescan-checkmark"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/codespaces-16 | codespaces icon}. */
export function codespaces(options?: SVGOptions): string {
  return octicons["codespaces"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/columns-16 | columns icon}. */
export function columns(options?: SVGOptions): string {
  return octicons["columns"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/command-palette-16 | command-palette icon}. */
export function commandPalette(options?: SVGOptions): string {
  return octicons["command-palette"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/comment-16 | comment icon}. */
export function comment(options?: SVGOptions): string {
  return octicons["comment"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/comment-ai-16 | comment-ai icon}. */
export function commentAi(options?: SVGOptions): string {
  return octicons["comment-ai"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/comment-discussion-16 | comment-discussion icon}. */
export function commentDiscussion(options?: SVGOptions): string {
  return octicons["comment-discussion"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/comment-locked-16 | comment-locked icon}. */
export function commentLocked(options?: SVGOptions): string {
  return octicons["comment-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/compose-16 | compose icon}. */
export function compose(options?: SVGOptions): string {
  return octicons["compose"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/container-16 | container icon}. */
export function container(options?: SVGOptions): string {
  return octicons["container"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/copilot-16 | copilot icon}. */
export function copilot(options?: SVGOptions): string {
  return octicons["copilot"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/copilot-error-16 | copilot-error icon}. */
export function copilotError(options?: SVGOptions): string {
  return octicons["copilot-error"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/copilot-warning-16 | copilot-warning icon}. */
export function copilotWarning(options?: SVGOptions): string {
  return octicons["copilot-warning"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/copy-16 | copy icon}. */
export function copy(options?: SVGOptions): string {
  return octicons["copy"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/cpu-16 | cpu icon}. */
export function cpu(options?: SVGOptions): string {
  return octicons["cpu"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/credit-card-16 | credit-card icon}. */
export function creditCard(options?: SVGOptions): string {
  return octicons["credit-card"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/cross-reference-16 | cross-reference icon}. */
export function crossReference(options?: SVGOptions): string {
  return octicons["cross-reference"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/crosshairs-16 | crosshairs icon}. */
export function crosshairs(options?: SVGOptions): string {
  return octicons["crosshairs"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/dash-16 | dash icon}. */
export function dash(options?: SVGOptions): string {
  return octicons["dash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/database-16 | database icon}. */
export function database(options?: SVGOptions): string {
  return octicons["database"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/dependabot-16 | dependabot icon}. */
export function dependabot(options?: SVGOptions): string {
  return octicons["dependabot"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/desktop-download-16 | desktop-download icon}. */
export function desktopDownload(options?: SVGOptions): string {
  return octicons["desktop-download"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/device-camera-16 | device-camera icon}. */
export function deviceCamera(options?: SVGOptions): string {
  return octicons["device-camera"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/device-camera-video-16 | device-camera-video icon}. */
export function deviceCameraVideo(options?: SVGOptions): string {
  return octicons["device-camera-video"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/device-desktop-16 | device-desktop icon}. */
export function deviceDesktop(options?: SVGOptions): string {
  return octicons["device-desktop"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/device-mobile-16 | device-mobile icon}. */
export function deviceMobile(options?: SVGOptions): string {
  return octicons["device-mobile"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/devices-16 | devices icon}. */
export function devices(options?: SVGOptions): string {
  return octicons["devices"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diamond-16 | diamond icon}. */
export function diamond(options?: SVGOptions): string {
  return octicons["diamond"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/dice-16 | dice icon}. */
export function dice(options?: SVGOptions): string {
  return octicons["dice"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-16 | diff icon}. */
export function diff(options?: SVGOptions): string {
  return octicons["diff"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-added-16 | diff-added icon}. */
export function diffAdded(options?: SVGOptions): string {
  return octicons["diff-added"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-ignored-16 | diff-ignored icon}. */
export function diffIgnored(options?: SVGOptions): string {
  return octicons["diff-ignored"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-modified-16 | diff-modified icon}. */
export function diffModified(options?: SVGOptions): string {
  return octicons["diff-modified"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-removed-16 | diff-removed icon}. */
export function diffRemoved(options?: SVGOptions): string {
  return octicons["diff-removed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/diff-renamed-16 | diff-renamed icon}. */
export function diffRenamed(options?: SVGOptions): string {
  return octicons["diff-renamed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/discussion-closed-16 | discussion-closed icon}. */
export function discussionClosed(options?: SVGOptions): string {
  return octicons["discussion-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/discussion-duplicate-16 | discussion-duplicate icon}. */
export function discussionDuplicate(options?: SVGOptions): string {
  return octicons["discussion-duplicate"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/discussion-outdated-16 | discussion-outdated icon}. */
export function discussionOutdated(options?: SVGOptions): string {
  return octicons["discussion-outdated"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/dot-16 | dot icon}. */
export function dot(options?: SVGOptions): string {
  return octicons["dot"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/dot-fill-16 | dot-fill icon}. */
export function dotFill(options?: SVGOptions): string {
  return octicons["dot-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/download-16 | download icon}. */
export function download(options?: SVGOptions): string {
  return octicons["download"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/duplicate-16 | duplicate icon}. */
export function duplicate(options?: SVGOptions): string {
  return octicons["duplicate"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/ellipsis-16 | ellipsis icon}. */
export function ellipsis(options?: SVGOptions): string {
  return octicons["ellipsis"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/exclamation-16 | exclamation icon}. */
export function exclamation(options?: SVGOptions): string {
  return octicons["exclamation"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/eye-16 | eye icon}. */
export function eye(options?: SVGOptions): string {
  return octicons["eye"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/eye-closed-16 | eye-closed icon}. */
export function eyeClosed(options?: SVGOptions): string {
  return octicons["eye-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-discussion-16 | feed-discussion icon}. */
export function feedDiscussion(options?: SVGOptions): string {
  return octicons["feed-discussion"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-forked-16 | feed-forked icon}. */
export function feedForked(options?: SVGOptions): string {
  return octicons["feed-forked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-heart-16 | feed-heart icon}. */
export function feedHeart(options?: SVGOptions): string {
  return octicons["feed-heart"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-issue-closed-16 | feed-issue-closed icon}. */
export function feedIssueClosed(options?: SVGOptions): string {
  return octicons["feed-issue-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-issue-draft-16 | feed-issue-draft icon}. */
export function feedIssueDraft(options?: SVGOptions): string {
  return octicons["feed-issue-draft"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-issue-open-16 | feed-issue-open icon}. */
export function feedIssueOpen(options?: SVGOptions): string {
  return octicons["feed-issue-open"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-issue-reopen-16 | feed-issue-reopen icon}. */
export function feedIssueReopen(options?: SVGOptions): string {
  return octicons["feed-issue-reopen"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-merged-16 | feed-merged icon}. */
export function feedMerged(options?: SVGOptions): string {
  return octicons["feed-merged"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-person-16 | feed-person icon}. */
export function feedPerson(options?: SVGOptions): string {
  return octicons["feed-person"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-plus-16 | feed-plus icon}. */
export function feedPlus(options?: SVGOptions): string {
  return octicons["feed-plus"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-public-16 | feed-public icon}. */
export function feedPublic(options?: SVGOptions): string {
  return octicons["feed-public"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-pull-request-closed-16 | feed-pull-request-closed icon}. */
export function feedPullRequestClosed(options?: SVGOptions): string {
  return octicons["feed-pull-request-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-pull-request-draft-16 | feed-pull-request-draft icon}. */
export function feedPullRequestDraft(options?: SVGOptions): string {
  return octicons["feed-pull-request-draft"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-pull-request-open-16 | feed-pull-request-open icon}. */
export function feedPullRequestOpen(options?: SVGOptions): string {
  return octicons["feed-pull-request-open"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-repo-16 | feed-repo icon}. */
export function feedRepo(options?: SVGOptions): string {
  return octicons["feed-repo"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-rocket-16 | feed-rocket icon}. */
export function feedRocket(options?: SVGOptions): string {
  return octicons["feed-rocket"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-star-16 | feed-star icon}. */
export function feedStar(options?: SVGOptions): string {
  return octicons["feed-star"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-tag-16 | feed-tag icon}. */
export function feedTag(options?: SVGOptions): string {
  return octicons["feed-tag"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/feed-trophy-16 | feed-trophy icon}. */
export function feedTrophy(options?: SVGOptions): string {
  return octicons["feed-trophy"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-16 | file icon}. */
export function file(options?: SVGOptions): string {
  return octicons["file"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-added-16 | file-added icon}. */
export function fileAdded(options?: SVGOptions): string {
  return octicons["file-added"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-badge-16 | file-badge icon}. */
export function fileBadge(options?: SVGOptions): string {
  return octicons["file-badge"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-binary-16 | file-binary icon}. */
export function fileBinary(options?: SVGOptions): string {
  return octicons["file-binary"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-check-16 | file-check icon}. */
export function fileCheck(options?: SVGOptions): string {
  return octicons["file-check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-code-16 | file-code icon}. */
export function fileCode(options?: SVGOptions): string {
  return octicons["file-code"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-diff-16 | file-diff icon}. */
export function fileDiff(options?: SVGOptions): string {
  return octicons["file-diff"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-directory-16 | file-directory icon}. */
export function fileDirectory(options?: SVGOptions): string {
  return octicons["file-directory"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-directory-fill-16 | file-directory-fill icon}. */
export function fileDirectoryFill(options?: SVGOptions): string {
  return octicons["file-directory-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-directory-open-fill-16 | file-directory-open-fill icon}. */
export function fileDirectoryOpenFill(options?: SVGOptions): string {
  return octicons["file-directory-open-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-directory-symlink-16 | file-directory-symlink icon}. */
export function fileDirectorySymlink(options?: SVGOptions): string {
  return octicons["file-directory-symlink"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-media-16 | file-media icon}. */
export function fileMedia(options?: SVGOptions): string {
  return octicons["file-media"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-moved-16 | file-moved icon}. */
export function fileMoved(options?: SVGOptions): string {
  return octicons["file-moved"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-removed-16 | file-removed icon}. */
export function fileRemoved(options?: SVGOptions): string {
  return octicons["file-removed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-submodule-16 | file-submodule icon}. */
export function fileSubmodule(options?: SVGOptions): string {
  return octicons["file-submodule"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-symlink-file-16 | file-symlink-file icon}. */
export function fileSymlinkFile(options?: SVGOptions): string {
  return octicons["file-symlink-file"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/file-zip-16 | file-zip icon}. */
export function fileZip(options?: SVGOptions): string {
  return octicons["file-zip"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/filter-16 | filter icon}. */
export function filter(options?: SVGOptions): string {
  return octicons["filter"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/filter-remove-16 | filter-remove icon}. */
export function filterRemove(options?: SVGOptions): string {
  return octicons["filter-remove"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/fiscal-host-16 | fiscal-host icon}. */
export function fiscalHost(options?: SVGOptions): string {
  return octicons["fiscal-host"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/flag-16 | flag icon}. */
export function flag(options?: SVGOptions): string {
  return octicons["flag"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/flame-16 | flame icon}. */
export function flame(options?: SVGOptions): string {
  return octicons["flame"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/flowchart-16 | flowchart icon}. */
export function flowchart(options?: SVGOptions): string {
  return octicons["flowchart"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/focus-center-16 | focus-center icon}. */
export function focusCenter(options?: SVGOptions): string {
  return octicons["focus-center"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/fold-16 | fold icon}. */
export function fold(options?: SVGOptions): string {
  return octicons["fold"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/fold-down-16 | fold-down icon}. */
export function foldDown(options?: SVGOptions): string {
  return octicons["fold-down"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/fold-up-16 | fold-up icon}. */
export function foldUp(options?: SVGOptions): string {
  return octicons["fold-up"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/gear-16 | gear icon}. */
export function gear(options?: SVGOptions): string {
  return octicons["gear"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/gift-16 | gift icon}. */
export function gift(options?: SVGOptions): string {
  return octicons["gift"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-branch-16 | git-branch icon}. */
export function gitBranch(options?: SVGOptions): string {
  return octicons["git-branch"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-branch-check-16 | git-branch-check icon}. */
export function gitBranchCheck(options?: SVGOptions): string {
  return octicons["git-branch-check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-commit-16 | git-commit icon}. */
export function gitCommit(options?: SVGOptions): string {
  return octicons["git-commit"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-compare-16 | git-compare icon}. */
export function gitCompare(options?: SVGOptions): string {
  return octicons["git-compare"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-merge-16 | git-merge icon}. */
export function gitMerge(options?: SVGOptions): string {
  return octicons["git-merge"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-merge-queue-16 | git-merge-queue icon}. */
export function gitMergeQueue(options?: SVGOptions): string {
  return octicons["git-merge-queue"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-pull-request-16 | git-pull-request icon}. */
export function gitPullRequest(options?: SVGOptions): string {
  return octicons["git-pull-request"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-pull-request-closed-16 | git-pull-request-closed icon}. */
export function gitPullRequestClosed(options?: SVGOptions): string {
  return octicons["git-pull-request-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-pull-request-draft-16 | git-pull-request-draft icon}. */
export function gitPullRequestDraft(options?: SVGOptions): string {
  return octicons["git-pull-request-draft"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/git-pull-request-locked-16 | git-pull-request-locked icon}. */
export function gitPullRequestLocked(options?: SVGOptions): string {
  return octicons["git-pull-request-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/globe-16 | globe icon}. */
export function globe(options?: SVGOptions): string {
  return octicons["globe"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/goal-16 | goal icon}. */
export function goal(options?: SVGOptions): string {
  return octicons["goal"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/grabber-16 | grabber icon}. */
export function grabber(options?: SVGOptions): string {
  return octicons["grabber"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/graph-16 | graph icon}. */
export function graph(options?: SVGOptions): string {
  return octicons["graph"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/graph-bar-horizontal-16 | graph-bar-horizontal icon}. */
export function graphBarHorizontal(options?: SVGOptions): string {
  return octicons["graph-bar-horizontal"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/graph-bar-vertical-16 | graph-bar-vertical icon}. */
export function graphBarVertical(options?: SVGOptions): string {
  return octicons["graph-bar-vertical"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/graph-stacked-area-16 | graph-stacked-area icon}. */
export function graphStackedArea(options?: SVGOptions): string {
  return octicons["graph-stacked-area"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/hash-16 | hash icon}. */
export function hash(options?: SVGOptions): string {
  return octicons["hash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/heading-16 | heading icon}. */
export function heading(options?: SVGOptions): string {
  return octicons["heading"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/heart-16 | heart icon}. */
export function heart(options?: SVGOptions): string {
  return octicons["heart"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/heart-fill-16 | heart-fill icon}. */
export function heartFill(options?: SVGOptions): string {
  return octicons["heart-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/history-16 | history icon}. */
export function history(options?: SVGOptions): string {
  return octicons["history"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/home-16 | home icon}. */
export function home(options?: SVGOptions): string {
  return octicons["home"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/home-fill-16 | home-fill icon}. */
export function homeFill(options?: SVGOptions): string {
  return octicons["home-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/horizontal-rule-16 | horizontal-rule icon}. */
export function horizontalRule(options?: SVGOptions): string {
  return octicons["horizontal-rule"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/hourglass-16 | hourglass icon}. */
export function hourglass(options?: SVGOptions): string {
  return octicons["hourglass"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/hubot-16 | hubot icon}. */
export function hubot(options?: SVGOptions): string {
  return octicons["hubot"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/id-badge-16 | id-badge icon}. */
export function idBadge(options?: SVGOptions): string {
  return octicons["id-badge"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/image-16 | image icon}. */
export function image(options?: SVGOptions): string {
  return octicons["image"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/inbox-16 | inbox icon}. */
export function inbox(options?: SVGOptions): string {
  return octicons["inbox"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/inbox-fill-16 | inbox-fill icon}. */
export function inboxFill(options?: SVGOptions): string {
  return octicons["inbox-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/infinity-16 | infinity icon}. */
export function infinity(options?: SVGOptions): string {
  return octicons["infinity"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/info-16 | info icon}. */
export function info(options?: SVGOptions): string {
  return octicons["info"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-closed-16 | issue-closed icon}. */
export function issueClosed(options?: SVGOptions): string {
  return octicons["issue-closed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-draft-16 | issue-draft icon}. */
export function issueDraft(options?: SVGOptions): string {
  return octicons["issue-draft"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-locked-16 | issue-locked icon}. */
export function issueLocked(options?: SVGOptions): string {
  return octicons["issue-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-opened-16 | issue-opened icon}. */
export function issueOpened(options?: SVGOptions): string {
  return octicons["issue-opened"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-reopened-16 | issue-reopened icon}. */
export function issueReopened(options?: SVGOptions): string {
  return octicons["issue-reopened"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-tracked-by-16 | issue-tracked-by icon}. */
export function issueTrackedBy(options?: SVGOptions): string {
  return octicons["issue-tracked-by"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/issue-tracks-16 | issue-tracks icon}. */
export function issueTracks(options?: SVGOptions): string {
  return octicons["issue-tracks"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/italic-16 | italic icon}. */
export function italic(options?: SVGOptions): string {
  return octicons["italic"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/iterations-16 | iterations icon}. */
export function iterations(options?: SVGOptions): string {
  return octicons["iterations"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/kebab-horizontal-16 | kebab-horizontal icon}. */
export function kebabHorizontal(options?: SVGOptions): string {
  return octicons["kebab-horizontal"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/key-16 | key icon}. */
export function key(options?: SVGOptions): string {
  return octicons["key"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/key-asterisk-16 | key-asterisk icon}. */
export function keyAsterisk(options?: SVGOptions): string {
  return octicons["key-asterisk"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/law-16 | law icon}. */
export function law(options?: SVGOptions): string {
  return octicons["law"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/light-bulb-16 | light-bulb icon}. */
export function lightBulb(options?: SVGOptions): string {
  return octicons["light-bulb"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/link-16 | link icon}. */
export function link(options?: SVGOptions): string {
  return octicons["link"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/link-external-16 | link-external icon}. */
export function linkExternal(options?: SVGOptions): string {
  return octicons["link-external"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/list-ordered-16 | list-ordered icon}. */
export function listOrdered(options?: SVGOptions): string {
  return octicons["list-ordered"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/list-unordered-16 | list-unordered icon}. */
export function listUnordered(options?: SVGOptions): string {
  return octicons["list-unordered"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/location-16 | location icon}. */
export function location(options?: SVGOptions): string {
  return octicons["location"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/lock-16 | lock icon}. */
export function lock(options?: SVGOptions): string {
  return octicons["lock"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/lockup-github-16 | lockup-github icon}. */
export function lockupGithub(options?: SVGOptions): string {
  return octicons["lockup-github"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/log-16 | log icon}. */
export function log(options?: SVGOptions): string {
  return octicons["log"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/logo-gist-16 | logo-gist icon}. */
export function logoGist(options?: SVGOptions): string {
  return octicons["logo-gist"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/logo-github-16 | logo-github icon}. */
export function logoGithub(options?: SVGOptions): string {
  return octicons["logo-github"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/loop-16 | loop icon}. */
export function loop(options?: SVGOptions): string {
  return octicons["loop"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mail-16 | mail icon}. */
export function mail(options?: SVGOptions): string {
  return octicons["mail"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mark-github-16 | mark-github icon}. */
export function markGithub(options?: SVGOptions): string {
  return octicons["mark-github"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/markdown-16 | markdown icon}. */
export function markdown(options?: SVGOptions): string {
  return octicons["markdown"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/maximize-16 | maximize icon}. */
export function maximize(options?: SVGOptions): string {
  return octicons["maximize"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mcp-16 | mcp icon}. */
export function mcp(options?: SVGOptions): string {
  return octicons["mcp"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/megaphone-16 | megaphone icon}. */
export function megaphone(options?: SVGOptions): string {
  return octicons["megaphone"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mention-16 | mention icon}. */
export function mention(options?: SVGOptions): string {
  return octicons["mention"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/meter-16 | meter icon}. */
export function meter(options?: SVGOptions): string {
  return octicons["meter"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/milestone-16 | milestone icon}. */
export function milestone(options?: SVGOptions): string {
  return octicons["milestone"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/minimize-16 | minimize icon}. */
export function minimize(options?: SVGOptions): string {
  return octicons["minimize"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mirror-16 | mirror icon}. */
export function mirror(options?: SVGOptions): string {
  return octicons["mirror"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/moon-16 | moon icon}. */
export function moon(options?: SVGOptions): string {
  return octicons["moon"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mortar-board-16 | mortar-board icon}. */
export function mortarBoard(options?: SVGOptions): string {
  return octicons["mortar-board"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/move-to-bottom-16 | move-to-bottom icon}. */
export function moveToBottom(options?: SVGOptions): string {
  return octicons["move-to-bottom"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/move-to-end-16 | move-to-end icon}. */
export function moveToEnd(options?: SVGOptions): string {
  return octicons["move-to-end"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/move-to-start-16 | move-to-start icon}. */
export function moveToStart(options?: SVGOptions): string {
  return octicons["move-to-start"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/move-to-top-16 | move-to-top icon}. */
export function moveToTop(options?: SVGOptions): string {
  return octicons["move-to-top"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/multi-select-16 | multi-select icon}. */
export function multiSelect(options?: SVGOptions): string {
  return octicons["multi-select"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/mute-16 | mute icon}. */
export function mute(options?: SVGOptions): string {
  return octicons["mute"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/no-entry-16 | no-entry icon}. */
export function noEntry(options?: SVGOptions): string {
  return octicons["no-entry"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/no-entry-fill-16 | no-entry-fill icon}. */
export function noEntryFill(options?: SVGOptions): string {
  return octicons["no-entry-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/node-16 | node icon}. */
export function node(options?: SVGOptions): string {
  return octicons["node"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/north-star-16 | north-star icon}. */
export function northStar(options?: SVGOptions): string {
  return octicons["north-star"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/note-16 | note icon}. */
export function note(options?: SVGOptions): string {
  return octicons["note"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/number-16 | number icon}. */
export function number(options?: SVGOptions): string {
  return octicons["number"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/organization-16 | organization icon}. */
export function organization(options?: SVGOptions): string {
  return octicons["organization"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/package-16 | package icon}. */
function _package(options?: SVGOptions): string {
  return octicons["package"].toSVG(options)
}
export { _package as package }

/** {@link https://primer.style/octicons/icon/package-dependencies-16 | package-dependencies icon}. */
export function packageDependencies(options?: SVGOptions): string {
  return octicons["package-dependencies"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/package-dependents-16 | package-dependents icon}. */
export function packageDependents(options?: SVGOptions): string {
  return octicons["package-dependents"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/paintbrush-16 | paintbrush icon}. */
export function paintbrush(options?: SVGOptions): string {
  return octicons["paintbrush"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/paper-airplane-16 | paper-airplane icon}. */
export function paperAirplane(options?: SVGOptions): string {
  return octicons["paper-airplane"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/paperclip-16 | paperclip icon}. */
export function paperclip(options?: SVGOptions): string {
  return octicons["paperclip"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/passkey-fill-16 | passkey-fill icon}. */
export function passkeyFill(options?: SVGOptions): string {
  return octicons["passkey-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/paste-16 | paste icon}. */
export function paste(options?: SVGOptions): string {
  return octicons["paste"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pause-16 | pause icon}. */
export function pause(options?: SVGOptions): string {
  return octicons["pause"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pencil-16 | pencil icon}. */
export function pencil(options?: SVGOptions): string {
  return octicons["pencil"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pencil-ai-16 | pencil-ai icon}. */
export function pencilAi(options?: SVGOptions): string {
  return octicons["pencil-ai"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/people-16 | people icon}. */
export function people(options?: SVGOptions): string {
  return octicons["people"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/person-16 | person icon}. */
export function person(options?: SVGOptions): string {
  return octicons["person"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/person-add-16 | person-add icon}. */
export function personAdd(options?: SVGOptions): string {
  return octicons["person-add"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/person-fill-16 | person-fill icon}. */
export function personFill(options?: SVGOptions): string {
  return octicons["person-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pin-16 | pin icon}. */
export function pin(options?: SVGOptions): string {
  return octicons["pin"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pin-slash-16 | pin-slash icon}. */
export function pinSlash(options?: SVGOptions): string {
  return octicons["pin-slash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pivot-column-16 | pivot-column icon}. */
export function pivotColumn(options?: SVGOptions): string {
  return octicons["pivot-column"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/play-16 | play icon}. */
export function play(options?: SVGOptions): string {
  return octicons["play"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/plug-16 | plug icon}. */
export function plug(options?: SVGOptions): string {
  return octicons["plug"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/plus-16 | plus icon}. */
export function plus(options?: SVGOptions): string {
  return octicons["plus"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/plus-circle-16 | plus-circle icon}. */
export function plusCircle(options?: SVGOptions): string {
  return octicons["plus-circle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/project-16 | project icon}. */
export function project(options?: SVGOptions): string {
  return octicons["project"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/project-check-16 | project-check icon}. */
export function projectCheck(options?: SVGOptions): string {
  return octicons["project-check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/project-roadmap-16 | project-roadmap icon}. */
export function projectRoadmap(options?: SVGOptions): string {
  return octicons["project-roadmap"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/project-symlink-16 | project-symlink icon}. */
export function projectSymlink(options?: SVGOptions): string {
  return octicons["project-symlink"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/project-template-16 | project-template icon}. */
export function projectTemplate(options?: SVGOptions): string {
  return octicons["project-template"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/pulse-16 | pulse icon}. */
export function pulse(options?: SVGOptions): string {
  return octicons["pulse"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/question-16 | question icon}. */
export function question(options?: SVGOptions): string {
  return octicons["question"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/quote-16 | quote icon}. */
export function quote(options?: SVGOptions): string {
  return octicons["quote"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/read-16 | read icon}. */
export function read(options?: SVGOptions): string {
  return octicons["read"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/redo-16 | redo icon}. */
export function redo(options?: SVGOptions): string {
  return octicons["redo"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/rel-file-path-16 | rel-file-path icon}. */
export function relFilePath(options?: SVGOptions): string {
  return octicons["rel-file-path"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/reply-16 | reply icon}. */
export function reply(options?: SVGOptions): string {
  return octicons["reply"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-16 | repo icon}. */
export function repo(options?: SVGOptions): string {
  return octicons["repo"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-clone-16 | repo-clone icon}. */
export function repoClone(options?: SVGOptions): string {
  return octicons["repo-clone"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-delete-16 | repo-delete icon}. */
export function repoDelete(options?: SVGOptions): string {
  return octicons["repo-delete"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-deleted-16 | repo-deleted icon}. */
export function repoDeleted(options?: SVGOptions): string {
  return octicons["repo-deleted"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-forked-16 | repo-forked icon}. */
export function repoForked(options?: SVGOptions): string {
  return octicons["repo-forked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-forked-locked-16 | repo-forked-locked icon}. */
export function repoForkedLocked(options?: SVGOptions): string {
  return octicons["repo-forked-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-locked-16 | repo-locked icon}. */
export function repoLocked(options?: SVGOptions): string {
  return octicons["repo-locked"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-pull-16 | repo-pull icon}. */
export function repoPull(options?: SVGOptions): string {
  return octicons["repo-pull"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-push-16 | repo-push icon}. */
export function repoPush(options?: SVGOptions): string {
  return octicons["repo-push"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/repo-template-16 | repo-template icon}. */
export function repoTemplate(options?: SVGOptions): string {
  return octicons["repo-template"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/report-16 | report icon}. */
export function report(options?: SVGOptions): string {
  return octicons["report"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/rocket-16 | rocket icon}. */
export function rocket(options?: SVGOptions): string {
  return octicons["rocket"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/rows-16 | rows icon}. */
export function rows(options?: SVGOptions): string {
  return octicons["rows"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/rss-16 | rss icon}. */
export function rss(options?: SVGOptions): string {
  return octicons["rss"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/ruby-16 | ruby icon}. */
export function ruby(options?: SVGOptions): string {
  return octicons["ruby"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sandbox-16 | sandbox icon}. */
export function sandbox(options?: SVGOptions): string {
  return octicons["sandbox"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/screen-full-16 | screen-full icon}. */
export function screenFull(options?: SVGOptions): string {
  return octicons["screen-full"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/screen-normal-16 | screen-normal icon}. */
export function screenNormal(options?: SVGOptions): string {
  return octicons["screen-normal"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/search-16 | search icon}. */
export function search(options?: SVGOptions): string {
  return octicons["search"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/server-16 | server icon}. */
export function server(options?: SVGOptions): string {
  return octicons["server"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/share-16 | share icon}. */
export function share(options?: SVGOptions): string {
  return octicons["share"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/share-android-16 | share-android icon}. */
export function shareAndroid(options?: SVGOptions): string {
  return octicons["share-android"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/shield-16 | shield icon}. */
export function shield(options?: SVGOptions): string {
  return octicons["shield"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/shield-check-16 | shield-check icon}. */
export function shieldCheck(options?: SVGOptions): string {
  return octicons["shield-check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/shield-lock-16 | shield-lock icon}. */
export function shieldLock(options?: SVGOptions): string {
  return octicons["shield-lock"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/shield-slash-16 | shield-slash icon}. */
export function shieldSlash(options?: SVGOptions): string {
  return octicons["shield-slash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/shield-x-16 | shield-x icon}. */
export function shieldX(options?: SVGOptions): string {
  return octicons["shield-x"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sidebar-collapse-16 | sidebar-collapse icon}. */
export function sidebarCollapse(options?: SVGOptions): string {
  return octicons["sidebar-collapse"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sidebar-expand-16 | sidebar-expand icon}. */
export function sidebarExpand(options?: SVGOptions): string {
  return octicons["sidebar-expand"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sign-in-16 | sign-in icon}. */
export function signIn(options?: SVGOptions): string {
  return octicons["sign-in"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sign-out-16 | sign-out icon}. */
export function signOut(options?: SVGOptions): string {
  return octicons["sign-out"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/single-select-16 | single-select icon}. */
export function singleSelect(options?: SVGOptions): string {
  return octicons["single-select"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/skip-16 | skip icon}. */
export function skip(options?: SVGOptions): string {
  return octicons["skip"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/skip-fill-16 | skip-fill icon}. */
export function skipFill(options?: SVGOptions): string {
  return octicons["skip-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sliders-16 | sliders icon}. */
export function sliders(options?: SVGOptions): string {
  return octicons["sliders"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/smiley-16 | smiley icon}. */
export function smiley(options?: SVGOptions): string {
  return octicons["smiley"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/smiley-frown-16 | smiley-frown icon}. */
export function smileyFrown(options?: SVGOptions): string {
  return octicons["smiley-frown"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/smiley-frustrated-16 | smiley-frustrated icon}. */
export function smileyFrustrated(options?: SVGOptions): string {
  return octicons["smiley-frustrated"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/smiley-grin-16 | smiley-grin icon}. */
export function smileyGrin(options?: SVGOptions): string {
  return octicons["smiley-grin"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/smiley-neutral-16 | smiley-neutral icon}. */
export function smileyNeutral(options?: SVGOptions): string {
  return octicons["smiley-neutral"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sort-asc-16 | sort-asc icon}. */
export function sortAsc(options?: SVGOptions): string {
  return octicons["sort-asc"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sort-desc-16 | sort-desc icon}. */
export function sortDesc(options?: SVGOptions): string {
  return octicons["sort-desc"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/space-16 | space icon}. */
export function space(options?: SVGOptions): string {
  return octicons["space"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/spacing-large-16 | spacing-large icon}. */
export function spacingLarge(options?: SVGOptions): string {
  return octicons["spacing-large"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/spacing-medium-16 | spacing-medium icon}. */
export function spacingMedium(options?: SVGOptions): string {
  return octicons["spacing-medium"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/spacing-small-16 | spacing-small icon}. */
export function spacingSmall(options?: SVGOptions): string {
  return octicons["spacing-small"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sparkle-16 | sparkle icon}. */
export function sparkle(options?: SVGOptions): string {
  return octicons["sparkle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sparkle-fill-16 | sparkle-fill icon}. */
export function sparkleFill(options?: SVGOptions): string {
  return octicons["sparkle-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sparkles-fill-16 | sparkles-fill icon}. */
export function sparklesFill(options?: SVGOptions): string {
  return octicons["sparkles-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/split-view-16 | split-view icon}. */
export function splitView(options?: SVGOptions): string {
  return octicons["split-view"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sponsor-tiers-16 | sponsor-tiers icon}. */
export function sponsorTiers(options?: SVGOptions): string {
  return octicons["sponsor-tiers"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/square-16 | square icon}. */
export function square(options?: SVGOptions): string {
  return octicons["square"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/square-circle-16 | square-circle icon}. */
export function squareCircle(options?: SVGOptions): string {
  return octicons["square-circle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/square-fill-16 | square-fill icon}. */
export function squareFill(options?: SVGOptions): string {
  return octicons["square-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/squirrel-16 | squirrel icon}. */
export function squirrel(options?: SVGOptions): string {
  return octicons["squirrel"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stack-16 | stack icon}. */
export function stack(options?: SVGOptions): string {
  return octicons["stack"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stack-add-16 | stack-add icon}. */
export function stackAdd(options?: SVGOptions): string {
  return octicons["stack-add"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stack-check-16 | stack-check icon}. */
export function stackCheck(options?: SVGOptions): string {
  return octicons["stack-check"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stack-remove-16 | stack-remove icon}. */
export function stackRemove(options?: SVGOptions): string {
  return octicons["stack-remove"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/star-16 | star icon}. */
export function star(options?: SVGOptions): string {
  return octicons["star"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/star-fill-16 | star-fill icon}. */
export function starFill(options?: SVGOptions): string {
  return octicons["star-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stop-16 | stop icon}. */
export function stop(options?: SVGOptions): string {
  return octicons["stop"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/stopwatch-16 | stopwatch icon}. */
export function stopwatch(options?: SVGOptions): string {
  return octicons["stopwatch"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/strikethrough-16 | strikethrough icon}. */
export function strikethrough(options?: SVGOptions): string {
  return octicons["strikethrough"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sun-16 | sun icon}. */
export function sun(options?: SVGOptions): string {
  return octicons["sun"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/sync-16 | sync icon}. */
export function sync(options?: SVGOptions): string {
  return octicons["sync"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tab-16 | tab icon}. */
export function tab(options?: SVGOptions): string {
  return octicons["tab"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tab-external-16 | tab-external icon}. */
export function tabExternal(options?: SVGOptions): string {
  return octicons["tab-external"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/table-16 | table icon}. */
export function table(options?: SVGOptions): string {
  return octicons["table"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tag-16 | tag icon}. */
export function tag(options?: SVGOptions): string {
  return octicons["tag"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tasklist-16 | tasklist icon}. */
export function tasklist(options?: SVGOptions): string {
  return octicons["tasklist"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/telescope-16 | telescope icon}. */
export function telescope(options?: SVGOptions): string {
  return octicons["telescope"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/telescope-fill-16 | telescope-fill icon}. */
export function telescopeFill(options?: SVGOptions): string {
  return octicons["telescope-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/terminal-16 | terminal icon}. */
export function terminal(options?: SVGOptions): string {
  return octicons["terminal"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/three-bars-16 | three-bars icon}. */
export function threeBars(options?: SVGOptions): string {
  return octicons["three-bars"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/thumbsdown-16 | thumbsdown icon}. */
export function thumbsdown(options?: SVGOptions): string {
  return octicons["thumbsdown"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/thumbsup-16 | thumbsup icon}. */
export function thumbsup(options?: SVGOptions): string {
  return octicons["thumbsup"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tools-16 | tools icon}. */
export function tools(options?: SVGOptions): string {
  return octicons["tools"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tracked-by-closed-completed-16 | tracked-by-closed-completed icon}. */
export function trackedByClosedCompleted(options?: SVGOptions): string {
  return octicons["tracked-by-closed-completed"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/tracked-by-closed-not-planned-16 | tracked-by-closed-not-planned icon}. */
export function trackedByClosedNotPlanned(options?: SVGOptions): string {
  return octicons["tracked-by-closed-not-planned"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/trash-16 | trash icon}. */
export function trash(options?: SVGOptions): string {
  return octicons["trash"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/triangle-down-16 | triangle-down icon}. */
export function triangleDown(options?: SVGOptions): string {
  return octicons["triangle-down"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/triangle-left-16 | triangle-left icon}. */
export function triangleLeft(options?: SVGOptions): string {
  return octicons["triangle-left"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/triangle-right-16 | triangle-right icon}. */
export function triangleRight(options?: SVGOptions): string {
  return octicons["triangle-right"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/triangle-up-16 | triangle-up icon}. */
export function triangleUp(options?: SVGOptions): string {
  return octicons["triangle-up"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/trophy-16 | trophy icon}. */
export function trophy(options?: SVGOptions): string {
  return octicons["trophy"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/typography-16 | typography icon}. */
export function typography(options?: SVGOptions): string {
  return octicons["typography"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/undo-16 | undo icon}. */
export function undo(options?: SVGOptions): string {
  return octicons["undo"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unfold-16 | unfold icon}. */
export function unfold(options?: SVGOptions): string {
  return octicons["unfold"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unlink-16 | unlink icon}. */
export function unlink(options?: SVGOptions): string {
  return octicons["unlink"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unlock-16 | unlock icon}. */
export function unlock(options?: SVGOptions): string {
  return octicons["unlock"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unmute-16 | unmute icon}. */
export function unmute(options?: SVGOptions): string {
  return octicons["unmute"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unread-16 | unread icon}. */
export function unread(options?: SVGOptions): string {
  return octicons["unread"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unverified-16 | unverified icon}. */
export function unverified(options?: SVGOptions): string {
  return octicons["unverified"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/unwrap-16 | unwrap icon}. */
export function unwrap(options?: SVGOptions): string {
  return octicons["unwrap"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/upload-16 | upload icon}. */
export function upload(options?: SVGOptions): string {
  return octicons["upload"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/verified-16 | verified icon}. */
export function verified(options?: SVGOptions): string {
  return octicons["verified"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/versions-16 | versions icon}. */
export function versions(options?: SVGOptions): string {
  return octicons["versions"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/video-16 | video icon}. */
export function video(options?: SVGOptions): string {
  return octicons["video"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/view-files-16 | view-files icon}. */
export function viewFiles(options?: SVGOptions): string {
  return octicons["view-files"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/vscode-16 | vscode icon}. */
export function vscode(options?: SVGOptions): string {
  return octicons["vscode"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/webhook-16 | webhook icon}. */
export function webhook(options?: SVGOptions): string {
  return octicons["webhook"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/workflow-16 | workflow icon}. */
export function workflow(options?: SVGOptions): string {
  return octicons["workflow"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/wrap-16 | wrap icon}. */
export function wrap(options?: SVGOptions): string {
  return octicons["wrap"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/x-16 | x icon}. */
export function x(options?: SVGOptions): string {
  return octicons["x"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/x-circle-16 | x-circle icon}. */
export function xCircle(options?: SVGOptions): string {
  return octicons["x-circle"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/x-circle-fill-16 | x-circle-fill icon}. */
export function xCircleFill(options?: SVGOptions): string {
  return octicons["x-circle-fill"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/zap-16 | zap icon}. */
export function zap(options?: SVGOptions): string {
  return octicons["zap"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/zoom-in-16 | zoom-in icon}. */
export function zoomIn(options?: SVGOptions): string {
  return octicons["zoom-in"].toSVG(options)
}

/** {@link https://primer.style/octicons/icon/zoom-out-16 | zoom-out icon}. */
export function zoomOut(options?: SVGOptions): string {
  return octicons["zoom-out"].toSVG(options)
}
